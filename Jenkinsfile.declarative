pipeline {
  agent any
    stages {
      stage('build') {
        steps {
          echo "building the code...."
        }
      }
      stage('test) {
        steps {
          echo "testing the build...."
        }
      }
   }
}
